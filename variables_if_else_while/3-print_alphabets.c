#include <stdio.h>
/**
 * main - main function
 * Return: Return 0
 * letter - letter to be incrimented
 */
int main(void)
{
	int letter;

	for (letter = 'a'; letter <= 'Z'; letter++)
	putchar(letter);
	{
		putchar('\n');
	}
		return (0);
}
