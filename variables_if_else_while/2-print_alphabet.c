#include <stdio.h>
/**
 * main - main function
 * Return: Return
 * letter - letter to be incrimented
 */
int main(void)
{
        int letter;

        for (letter = 'a'; letter <= 'z'; letter++)
                putchar(letter);
	{
		putchar('\n');
	}
		return (0);
}
