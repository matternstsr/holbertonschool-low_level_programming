#include <stdio.h>
/**
 * main - main function
 * Return: Return 0
 * letter - letter to be incrimented
 */
int main(void)
{
	int decimal;

	for (decimal = 0; decimal <= 9; decimal++)
	{
		printf("%d", decimal);
	}
	putchar('\n');
	return (0);
}
