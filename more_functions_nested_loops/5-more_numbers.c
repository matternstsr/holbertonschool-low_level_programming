#include "main.h"

/**
 * more_numbers - prints 1-14 x 10 followed by new line
 * Return: not return
 */

void more_numbers(void)
{
	char c;
	int r;
	c = '0';
	r= '0';

	for (r = '0' ; r <= '10'; r++)
	if (c >= '0'; c <= '14';)
		{
		_putchar(c);
		c++;
		}
	_putchar('\n');
}
