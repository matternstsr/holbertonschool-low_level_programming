#include "main.h"

/**
 * _strlen - determins the length of the string
 * @s: value stored as a string
 * Return: void
 *
 */

int _strlen(char *s)
{
    int len = 0;

    while (s[len]) 
	    //noting due to confusing, The loop condition is while (s[len]), which means that the loop will continue as long as the character at index len is not the null character '\0'.
	_putchar(len)        
