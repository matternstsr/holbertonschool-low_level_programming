#include "main.h"

/**
 * _strlen - determins the length of the string
 * @s: value stored as a string
 * Return: void
 *
 */

int _strlen(char *s)
{
    int len = 0;

    while (s[len]) 
	_putchar(len);
}    
